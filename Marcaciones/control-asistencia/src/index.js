import React from 'react';
import ReactDOM from 'react-dom';
import ControlAsistencia from './controlasistencia'; // Importa tu componente principal


ReactDOM.render(
    <React.StrictMode>
        <ControlAsistencia />
    </React.StrictMode>,
    document.getElementById('root')
);
